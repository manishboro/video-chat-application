import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  formLabel: {
    fontFamily: "Montserrat !important",
    color: "#434343",
    // fontSize: "1.6rem",
  },

  formHelperText: { fontSize: "1.6rem", padding: ".5rem 0 0 1rem", margin: 0 },

  textField: {
    width: "100% !important",
    fontFamily: "Montserrat !important",
    backfaceVisibility: "hidden",
    fontSize: "1.6rem",
  },
});
