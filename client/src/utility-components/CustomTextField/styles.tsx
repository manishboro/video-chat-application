import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  formLabel: { fontFamily: "Montserrat !important", color: "#434343" },

  formHelperText: { fontSize: 20, padding: ".5rem 0 0 1rem", margin: 0 },

  textField: { width: "100% !important", fontFamily: "Montserrat !important", backfaceVisibility: "hidden" },
});
